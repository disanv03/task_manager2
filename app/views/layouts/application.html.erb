<!DOCTYPE html>
<html>
  <head>
    <title>TaskManager<%= ' - ' + yield(:title) if content_for?(:title) %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield(:script) %>
  </head>

  <body>
    <%# here content will appear in every page %>
    <header>

    <%# link_to(menu_path) do %>
        <%# image_tag('task_manager_logo.png', height: 25, alt: 'Task Manager Logo') %>
    <%# end %> 

    <%= link_to(image_tag('task_manager_logo.png', height: 25, alt: 'Task Manager'), menu_path) %>
    <% if session[:user_id].present? %>
    <span>
      User: <%= @username %> | <%= button_to('Logout', logout_path, {method: :delete})%>
    </span>
    <% end %>
    </header>

    <%# that where those template are drop in %>
    <div>
      <%= render(partial: 'layouts/flash') %> 
      <%= yield %>

    </div>

    <footer>
    Copyright <%= Time.now.year %>
    </footer>
  </body>
</html>
